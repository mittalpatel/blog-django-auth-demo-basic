Demo of Django Authentication.

clone the repo and install the requirements from req.txt file.

Run the migrations to create the database.

Run the django server. and open the ``localhost:8000/register`` to register the user. and for login open the ``localhost:8000/login`` page for the login functionality.